name: seo_seed_kaisel
description: kaisel adapter for seo_seed — map sealed kaisel routes to SEO pages via the router's URL codec.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  kaisel_core: ^0.21.0
  seo_seed: ^0.1.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
