name: seo_seed_go_router_example
description: "Flutter web app using go_router, with SEO via seo_seed_go_router."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  seo_seed: ^0.1.0
  seo_seed_go_router: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
