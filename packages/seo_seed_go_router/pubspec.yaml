name: seo_seed_go_router
description: go_router adapter for seo_seed — declare a route's SEO facet once, alongside its path.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0
  flutter: ">=3.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  seo_seed: ^0.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
